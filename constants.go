package main

// The file mode with which new pastes are written.
// The owner can read file file. That is all.
const pastePerm = 0400

// URLs for stored pastes are rooted here
const pasteRoot string = "/paste/"
